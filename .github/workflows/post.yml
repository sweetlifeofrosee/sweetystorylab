name: Horror Reels Bot

on:
  schedule:
    - cron: '0 11 * * *'  # 7PM PHT only
  workflow_dispatch:        # Manual trigger for testing

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests Pillow edge-tts

      - name: Install FFmpeg
        run: sudo apt-get install -y ffmpeg

      - name: Download Lora font
        run: |
          curl -L "https://github.com/google/fonts/raw/main/ofl/lora/Lora%5Bwght%5D.ttf" \
            -o /tmp/Lora-Italic.ttf

      - name: Run horror reels bot
        env:
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python post.py

      - name: Save log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: horror-log
          path: horror_log.db
          retention-days: 30
